<div class="jumbotron jumbotron-fluid" style="background-color: transparent">
  <div class="container text-center">
    <h1 class="display-3">Tired of corporate greed?</h1>
    <br />
    <p class="lead">...us too.</p>
    <br />
    <br />
    <p class="lead">
      Collectively, we can hold the worst offenders accountable and remind ourselves that some companies are still trying to help.
    </p>
  </div>
</div>
<div>
  <% if @events.any? %>
    <h1>What is happening around the world?</h1>
    <% @events.each do |event| %>
      <div class="card" style="width: 18rem">
        <img src="<%= url_for event.image %>" alt="<%= event.name %>" class="card-img-top">
        <div class="card-body">
          <h5 class="card-title"><%= event.name %></h5>
          <p class="card-text"><%= truncate(event.synopsis, length: 100) %></p>
          <%= link_to "Read more", event_path(event.id), class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  <% end %>
  <% if @submissions.any? %>
    <h1>Check out the latest submissions</h1>
    <ol>
      <% @submissions.each do |submission| %>
      <li><a href="<%= submission.url %>"><%= submission.url %></a></li>
      <% end %>
    </ol>
  <% end %>
</div>
